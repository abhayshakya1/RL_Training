[project]
name = "rl-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abhay Shakya", email = "abhayshakya10@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "verifiers>=0.3.1",
]

[project.scripts]
rl-training = "rl_training:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
